# Tomb of Doom — web roadmap

SPA en React + Vite + Tailwind + lucide-react. Sitio en GitHub Pages: `https://coriamaximiliano.github.io/Tomb-of-Doom/` (push a `main`, ver `.github/workflows/deploy.yml` e `INSTRUCCIONES_PAGINA_ROADMAP.txt`).
